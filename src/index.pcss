body {
  margin: 0;
}

.modal {
  position: relative;

  &__overlay {
    position: fixed;
    width: 100%;
    height: 100%;
    background-color: lightblue;
  }

  &__content {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
  }
}